name: 'Changelog generator'
description: 'Generate a changelog for your PR'
inputs:
  trigger:
    description: 'The trigger to generate the PR. Could be either "[KEYWORD]", "always" or "none"'
    required: false
    default: 'always'
runs:
  using: 'node12'
  main: 'index.js'
